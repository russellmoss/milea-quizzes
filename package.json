{
  "name": "milea-quiz-app",
  "version": "1.0.2",
  "description": "Quiz application for Milea Estate Training",
  "main": "index.js",
  "scripts": {
    "build": "npm install && node build.js",
    "vercel-build": "npm install && node build.js",
    "dev": "npx tailwindcss -i ./src/input.css -o ./output.css --watch"
  },
  "dependencies": {
    "autoprefixer": "^10.4.16",
    "cssnano": "^6.0.3",
    "dotenv": "^16.3.1",
    "firebase": "^10.7.1",
    "postcss": "^8.4.32",
    "postcss-cli": "^11.0.0",
    "tailwindcss": "^3.4.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
